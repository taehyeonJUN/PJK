<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.3.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing 
		infrastructure -->

	<!-- Enables the Spring MVC @Controller programming model -->
	<!-- 어노테이션을 사용하여 Spring MVC 애플리케이션을 작성할때 필요한 객체들을 자동 등록 -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving 
		up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources 
		in the /WEB-INF/views directory -->
	<!-- 컨트롤러가 문자열을(뷰 이름) 반환한 경우, 그 문자열의 앞에 "/WEB-INF/views/"를 붙이고 뒤에 ".jsp"를 
		붙여서 -->
	<!-- 해당 문자열의 jsp파일 경로로 이동(forward)시키는 역할을 수행 -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>


	<!-- kr.ac.kopo 패키지(폴더) 아래에 있는 모든 클래스들을 조사하여, -->
	<!-- @Component, @Comtroller, @Service, @Repository 등이 붙어있는 클래스들을 스프링(DispatcherServlet)에 
		자동 등록 -->
	<context:component-scan
		base-package="com.edu.pjk" />

	<!-- @Controller : 웹 요청을 받았을 때 실행되는 메서드를 담고 있는 객체에 사용 -->
	<!-- @Service : 서비스 계층의 객체에 사용 -->
	<!-- @Repository : DAO계층의 객체에 사용 -->


	<interceptors>
		<interceptor>
<!-- 			모든 경로에서 인터셉터 실행 -->
			<mapping path="/**" />
<!-- 			인터셉트제외 사항 -->
			<exclude-mapping path="/" />
			<exclude-mapping path="/mem/add" />
<!-- 			실행하는 인터셉터 -->
			<beans:bean class="com.edu.pjk.Login.LoginInterceptor"></beans:bean>
		</interceptor>
	</interceptors>



	

<!-- 	com.edu.pjk 패키지(폴더) 아래에 존재하는 @Mapper가 존재하는 인터페이스를  찾아서 그 인터페이스의 구현체를 마이바티스 구현체를 자동 생성하여 스프링에 등록 -->
	<mybatis-spring:scan base-package="com.edu.pjk" annotation="org.apache.ibatis.annotations.Mapper"/>
	
	
	
</beans:beans>
